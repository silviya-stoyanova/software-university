import React from 'react'

const Home = () => {
    return (
        <div className="home-page">
            <h1 className="heading">Wellcome to SeenIt !</h1>
            <h2 className="author">This is a Web application (SPA) using HTML5, JavaScript, AJAX, REST and JSON with cloud-based backend (Kinvey).
             The app keeps users, posts and comments. Users can register, login, logout, view a catalog with all posts sorted by time
             (a helper function will be given), create a post, edit/delete their own posts, view a post with it’s comments, create a comment,
             delete their own comments and a section where they can view their own posts.</h2>
        </div>
    )
}

export default Home